import { SidebarHeaderItemDirective } from './sidebar-header-item.directive';

describe('SidebarHeaderItemDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarHeaderItemDirective();
    expect(directive).toBeTruthy();
  });
});
